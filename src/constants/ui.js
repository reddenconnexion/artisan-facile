// Shared Tailwind class strings used across the auth flows so the styling
// stays consistent without copy-pasting long class lists between files.

export const AUTH_INPUT_CLASS =
    'block w-full px-4 py-3 border border-gray-300 rounded-xl text-gray-900 placeholder-gray-400 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent text-sm transition-shadow';
